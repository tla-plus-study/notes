Description
===========

Dump of notes for our TLA+ study group.

* Notes are organized in the `notes/` folder
* Run `scripts/new-week.sh` to generate a new week folder and `make` to generate pdf files (needs `pdflatex` installed)
